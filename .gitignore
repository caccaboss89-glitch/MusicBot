node_modules/
CLAUDE.md
AGENTS.md
.env
logs/
temp/
audio_engine/target/
audio_engine/Cargo.lock
package-lock.json

# Runtime-only state. Everything else under data/ (stats.json, playlists.json,
# monthly-stats/) is versioned on purpose: it is the monthly snapshot published
# by the stats site and the playlist backup.
data/queue_backup.json
data/pushState.json
